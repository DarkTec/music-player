import React from 'react';
import { render } from '@testing-library/react';

import Dashboard from '.';

test('renders add folder form', () => {});
test('can add folder', () => {});
test('renders table of folders', () => {});
test('can correctly scan library', () => {});
test('can remove folder', () => {});